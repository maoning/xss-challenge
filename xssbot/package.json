{
  "name": "xssbot",
  "version": "1.0.0",
  "description": "A node server that runs xssbots which are orchestrated by puppeteer using headlness chrome.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "anniemao",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "puppeteer": "^6.0.0"
  }
}
